// ci.jenkins.io build (used once hosted under jenkinsci in Phase 2).
buildPlugin(
    useContainerAgent: true,
    configurations: [
        [platform: 'linux', jdk: 21],
        [platform: 'windows', jdk: 17],
    ]
)
